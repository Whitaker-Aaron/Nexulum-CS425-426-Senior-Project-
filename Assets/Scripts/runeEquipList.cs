using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RuneEquipList : MonoBehaviour
{
    [SerializeField] public List<Rune> allRunes = new List<Rune>();
    // Start is called before the first frame update
}
