using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EquippedRunePrefab : MonoBehaviour
{
    [SerializeField] public GameObject runeName;
    [SerializeField] public GameObject runeType;
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
