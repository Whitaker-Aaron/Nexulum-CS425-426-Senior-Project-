using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemsEquipList : MonoBehaviour
{
    [SerializeField] public List<PlayerItem> allItems = new List<PlayerItem>();
    // Start is called before the first frame update
}
